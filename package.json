{
  "name": "tempest-jrpg",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "description": "Tempest – Chronik: ein JRPG (Phaser 4 + TypeScript) für Browser & Handy.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:smoke": "playwright test",
    "test:shard": "vitest run --shard",
    "test:e2e:shard": "playwright test --shard",
    "test:watch": "vitest",
    "test:ci": "vitest run --bail=1"
  },
  "dependencies": {
    "phaser": "^4.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
